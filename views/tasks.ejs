<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tasks</title>
</head>
<body>
    <h1>Task List</h1>
    <ul>
        <% tasks.forEach(task => { %>
            <li><%= task.title %></li>
        <% }); %>
    </ul>
    <a href="/add-Task">Add Another Task</a>
</body>
</html>
